<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>바닐라코딩: CodeWars</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script defer src="/javascripts/loading.js"></script>
  </head>
  <body>
    <div class="loginContainer">
      <h1>Login</h1>

      <form class="login" action="/login" method="post" onsubmit="handleSubmit('loading-login', '.submitButton')">
        <div class="inputBox">
          <input
            class="input"
            type="text"
            name="id"
            placeholder="ID"
            autofocus
          />
          <input
            class="input"
            type="password"
            name="password"
            placeholder="Password"
          />
          <% if (error) { %>
            <div class="error">
              <%= error %>
            </div>
          <% } %>
        </div>
        <button class="submitButton" type="submit">
          로그인
        </button>
        <div id="loading-login" class="loading">로그인 중 입니다. 잠시만 기다려 주세요.</div>
      </form>
      <div class="divider"></div>
      <div class="buttonBox">
        아직 회원이 아니신가요?
        <a href="/signup">
          <button class="toSignupButton">
            회원가입
          </button>
        </a>
      </div>
    </div>
  </body>
</html>
