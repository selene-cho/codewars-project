<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>바닐라코딩: CodeWars</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script defer src="/javascripts/loading.js"></script>
  </head>
  <body>
    <div class="signupContainer">
      <h1>Sign Up</h1>

      <form
        class="signup"
        action="/signup"
        method="post"
        onsubmit="handleSubmit('loading-signup', '.submitButton')"
        novalidate
      >
        <div class="inputBox">
          <input
            class="input"
            type="text"
            name="id"
            placeholder="ID"
            required
          />
          <% if (error && error.id) { %>
            <div class="error">
              <%= error.id %>
            </div>
          <% } %>
          <input
            class="input"
            type="password"
            name="password"
            placeholder="Password"
            required
          />
          <% if (error && error.password) { %>
            <div class="error">
              <%= error.password %>
            </div>
          <% } %>
        </div>
        <% if (error && error.general) { %>
          <div class="error">
            <%= error.general %>
          </div>
        <% } %>
        <button type="submit" class="submitButton">
          회원가입
        </button>
        <div id="loading-signup" class="loading">회원가입 중 입니다. 잠시만 기다려 주세요.</div>
      </form>
      <div class="divider"></div>
      <div class="buttonBox">
        이미 가입하셨나요?
        <a href="/login">
          <button class="toLoginButton">
            로그인하러 가기
          </button>
        </a>
      </div>
    </div>
  </body>
</html>
